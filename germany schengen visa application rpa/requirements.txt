# Pinned versions: previously used >= which caused Railway redeploys (e.g. after
# the ZERP-55 merge) to silently upgrade Playwright from 1.40 to 1.59 and pull
# Chromium 147 instead of the bundled Chromium 123, breaking VIDEX automation.
# Keep these pinned to the tested stack.
playwright==1.40.0
pydantic==2.7.4
rich==13.7.1
pypdf==4.2.0
fastapi==0.111.0
uvicorn[standard]==0.30.1
python-multipart==0.0.9
redis==5.0.7
